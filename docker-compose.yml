version: '3.8'

services:
  redactor-for12:
    build: .
    container_name: redactor_app
    ports:
      - "5003:5000"
    volumes:
      - .:/app
    environment:
      - VIRTUAL_HOST=redactor.adantechti.com
    restart: always